export const environment = {
  production: false,
  apiUrl: '/api/v1',
  /** URL absoluta del `landing-page/preview.html` servido con Live Server / `npx serve` (Semana 8). */
  previewLandingUrl: 'http://127.0.0.1:5500/landing-page/preview.html',
};
